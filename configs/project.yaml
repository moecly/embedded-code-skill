project:
  name: "my_firmware"
  build_cmd: "make -j4"
  output: "build/firmware.elf"

flasher:
  type: "jlink"
  device: "STM32F103RC"
  speed: 4000
  interface: "SWD"

serial:
  port: "COM3"
  baudrate: 115200
  timeout: 5

debug:
  max_retries: 3
  log_dir: "logs"
